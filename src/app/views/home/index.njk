{% extends "layout.njk" %}

{% block content%}
    <div class="wrapper">
        <div class="wrapper-content">
            <h1 class="wrapper-title">As melhores receitas</h1>
            <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.
            </p>
        </div>
        <div class="wrapper-img">
            <img src="/assets/chef.png" alt="Imagem animada do chef">
        </div>
    </div>

    <h2 id="mais-acessadas">Mais acessadas</h2>

    <section class="cards">
        {% for item in items %}

            <div class="card" id="{{item.id}}">
                <div class="card_img-content">
                    <img src="{{item.image}}" alt="{{item.title}}">
                </div>
                <div class="card-content">
                    <p class="card-title">{{item.title}}</p>
                    <p class="card-autor">{{item.chef_name}}</p>
                </div>
            </div>

        {% endfor %}

    </section>

    <script src="/scripts.js"></script>

{% endblock %}


